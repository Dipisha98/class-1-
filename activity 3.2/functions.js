//Create variable holding the student marks.
//Create a variable holding the total marks.
//Divide the first variable by the second and multiply that quotient by 100.

marksearned=98
percentage=100

function calculate (marksearned,percentage)
{console.log("Total grade or marks: " + marksearned/percentage*100+"%")}

calculate(marksearned,percentage)
